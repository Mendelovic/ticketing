apiVersion: skaffold/v4beta4
kind: Config
metadata:
  name: ticketing
build:
  local:
    push: false
  artifacts:
    - image: mendelovic/auth
      context: auth
      docker:
        dockerfile: Dockerfile
      sync:
        manual:
          - src: "src/**/*.ts"
            dest: .
manifests:
  rawYaml:
    - infra/k8s/*
